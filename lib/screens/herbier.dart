import 'package:flutter/material.dart';

class HerbierPage extends StatelessWidget {
  const HerbierPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Herbier',
        style: TextStyle(
          fontSize: 24,
          fontWeight: FontWeight.w600,
        ),
      ),
    );
  }
}
